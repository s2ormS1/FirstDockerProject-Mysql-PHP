server {
	listen 80;
	server_name _;
	
	root /var/www/html;
	index mainPage.php;

	location / {
		try_files $uri $uri/ /mainPage.php?$query_string;
	}

	location ~ \.php$ {
		include fastcgi_params;
		fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
		fastcgi_pass php:9000;
	}
}
